# 사랑나라 작업일지

> 피아노를 쳐서 사랑나라를 예쁘게 만드는 웹 게임 (PWA)

## 배포 정보
- **저장소**: https://github.com/SYA-Apps/sarangnara (public)
- **게임 주소**: https://sya-apps.github.io/sarangnara/
- **로컬 폴더**: `C:\SYA\Sarangnara\sarangnara-web`
- **현재 버전(웹)**: `sarangnara-v53` — `sw.js` 의 VER 과 `index.html` 의 `APP_VER`
  **두 곳을 같은 값으로** 맞춘다. 이 숫자가 타이틀 화면 왼쪽 아래에 그대로 보인다(2026-08-29).
- **연락처는 전부 개발자 계정 주소로 통일** — 개인 메일은 공개된 곳 어디에도 넣지 않는다.
  (주소 자체는 로컬 메모리에. 이 파일은 공개 저장소라 안 적는다)
- 🚫 **이 파일은 공개 저장소에 그대로 올라간다 — 사람 이름·이메일·전화번호를 적지 않는다.**
  테스터 명단 원본은 **Play Console 의 테스터 목록**, 인원·경과일 추적은
  `C:\SYA\tools\테스터-관리표.html`(git 저장소 아님). 개인 주소가 필요하면 거기서 본다.
  (2026-07-24: 테스터 3명의 이름·주소를 여기 적었다가 `git filter-repo` 로 히스토리까지 다시 쓰고
  강제 푸시해 지웠다. 판단 기준은 **"그 명단이 비공개인가"가 아니라 "적는 장소가 공개인가"** 다.
  콘솔 테스터 목록이 비공개인 건 맞지만, 그걸 옮겨 적은 이 파일이 공개였다.)
- 커밋 작성자: `SYA <sya@noreply.invalid>` (reading-log 등 다른 프로젝트와 동일)

## 배포 방법 (파일 고친 뒤)
```
git add -A
git commit -m "무엇을 바꿨는지"
git push
```
- push 하면 1~2분 뒤 자동으로 사이트 반영
- ~~게임을 고칠 때마다 `sw.js` 의 VER 숫자를 1 올릴 것~~ → **2026-08-28 부터 안 올려도 된다.**
  홈 화면에 추가한 사람도 앱을 다시 열면 저절로 최신이 된다(아래 08-28 항목).
  VER 은 이제 캐시 서랍 이름일 뿐이라, 캐시를 통째로 비우고 싶을 때만 올린다.

## 📏 앱과 웹은 **언제나 같은 기능**이어야 한다 (2026-08-30 사용자 지시)
> **웹에 있는 기능은 앱에도, 앱에 있는 기능은 웹에도 — 늘 같이 바꾼다.** 한쪽만 고치지 않는다.
> 구조상 이미 `sarangnara-web/index.html` **한 벌이 원본**이고 `npm run sync` 로 앱에 복사되므로,
> 기능을 **환경별로 두 벌 짜지 말고 한 함수 안에서 갈래를 나눈다**(v53 `shareApp()` 이 본보기).
>
> 🚨 **그런데 "같은 코드" 가 "같은 동작" 을 뜻하지는 않는다.** 앱은 **안드로이드 WebView**,
> 웹은 **크롬·사파리** 라서 **쓸 수 있는 브라우저 기능이 다르다.** 실제로 v53 에서
> `navigator.share` 가 웹에는 있고 WebView 에는 없어서, 같은 코드인데도 앱에서만
> 공유 시트가 안 떴다. → **웹 API 를 새로 쓸 때마다 "WebView 에도 있나" 를 확인할 것.**
> 확인은 `check-share.mjs` 처럼 **Capacitor 를 흉내 내어 양쪽 갈래를 다 눌러 보는** 식으로 한다.

## ⚠️ 앱(AAB) 반영 현황
> 웹(GitHub Pages)은 push 로 바로 바뀌지만, **앱은 웹을 복사해 동봉**한 거라 자동으로 안 따라온다.
> 웹만 고치고 끝내면 테스터 폰에는 안 닿는다 — 반드시 `npm run sync` → 버전 올림 → `npm run release`.

> 🚀 **2026-08-03: versionCode 3(=versionName 1.2) AAB 빌드 완료 — 사람이 콘솔에 올리는 것만 남음.**
> 웹 **v49** 스냅샷 동봉(폰 가로 `사랑나라로` 버튼 잘림 수정). 파일:
> `sarangnara-app/android/app/build/outputs/bundle/release/app-release.aab`
> 검증: 매니페스트 `versionCode=3` · `versionName=1.2`, `jarsigner -verify` → **jar verified**,
> www 안에 v49 CSS 들어간 것 확인, **`uses-permission` 은 여전히 0개**(INTERNET 없음).
> (`androidx.profileinstaller` 의 `android.permission.DUMP` 는 리시버를 부를 쪽에 요구하는 것이지
> 우리가 요청하는 권한이 아니다 — versionCode 1·2 에도 있었다.)
> ⚠️ `verify-build.mjs` 는 **폰 웹뷰에 붙는 도구**라 릴리스 AAB 검증엔 못 쓴다(9222 ECONNREFUSED).
> AAB 는 위처럼 `unzip -p ... base/manifest/AndroidManifest.xml` + `jarsigner -verify` 로 본다.

> ✅ **2026-07-25: versionCode 2(=versionName 1.1) 업로드·검토 제출 완료.** 웹 **v48** 스냅샷 동봉
> (악보 안 보임 수정 + 엔딩 버튼 크기 통일 + v42~v46 공유/OG). 빌드: `npm run release`.
> 비공개 트랙 `사랑나라_260721` 의 빈 임시 버전을 `버전 수정`으로 재사용해 AAB 업로드 →
> 출시 노트 입력 → 저장 → **게시 개요에서 "검토를 위해 변경사항 전송" 클릭 → 상태 "검토 중인 변경사항".**
> 경고 1개(가독화 파일 미포함)는 무해, 오류 0. 심사 보통 7일 이내. **14일 옵트인 카운트는 리셋 안 됨.**
> 게시되면 테스터 폰 Play 스토어가 업데이트 배포(지울 필요 없이 "업데이트").
>
> ✅ **2026-08-01: versionCode 2 게시 확인.** 비공개 테스트 트랙 `사랑나라_260721` 에
> **버전 2 (1.1) · 최종 업데이트 7월 25일 · "선택한 테스터에게 제공됩니다"**.
> **제출 당일 통과했고 이번에도 메일은 오지 않았다** — 1.0 때와 같은 패턴이다.
> **비공개 테스트는 Play 가 심사 결과 메일을 안 보낸다고 보면 된다. 콘솔만 본다.**
> 트랙 목록에서 버전 번호가 초록 체크와 함께 보이면 게시된 것이고, 검토 중이면 그 자리에
> "검토 중" 이 뜨고 버전이 안 올라온다. (`비공개 테스트 - Alpha` 는 Play 가 기본으로 만들어 둔
> 빈 트랙이라 무시한다. 우리가 쓰는 트랙은 `사랑나라_260721` 하나뿐.)
> ✅ **2026-08-03: 갤S8 앱 실물 확인 완료.** 폰에 깔린 것이 versionCode 2 / 1.1, 설치 경로
> `com.android.vending`(Play), 7/27 업데이트인 것까지 `dumpsys` 로 확인하고 실제로 눌러 봤다.
> **악보 정상**(크고 또렷, 아래 안내 문구 안 덮음, 잘림 없음) — v47 수정이 앱에 닿은 것 확인.
> **엔딩 버튼도 세 화면 크기 전부 192×37 동일**(v48). 타이틀·지도·대화, 공유 링크, 법적 링크 정상.
> 대신 **새 버그를 하나 찾았다 → 아래 08-03 항목(v49).**
>
> 📌 **릴리스 앱은 원격 디버깅이 안 되므로**(디버그 빌드가 아니다) `walk-phone.mjs` 류가 못 붙는다.
> `adb shell input tap` + `adb exec-out screencap` 으로 눌러가며 눈으로 보는 수밖에 없다.
> 🚫 **확인하겠다고 디버그 APK 를 깔지 말 것** — 서명이 달라 덮어쓰기가 안 되고, 깔려면 Play 버전을
> 지워야 한다. 이 폰이 옵트인 12명 중 하나면 **설치 수가 깎여 14일 카운트가 리셋**된다.
> 💡 **대신 폰 크롬으로 웹을 열면 된다.** 이 기기는 크롬도 **웹뷰와 같은 72.0.3626.121** 이라
> 앱 웹뷰와 같은 엔진으로 검증된다. 크롬은 릴리스 앱과 달리 원격 디버깅이 열려 있어
> `adb forward tcp:9222 localabstract:chrome_devtools_remote` 로 붙어 **직접 재는 것까지** 된다.
> (앱은 `webview_devtools_remote_<pid>`, 크롬은 `chrome_devtools_remote` — 소켓 이름이 다르다.)

**Play Console 에 게시된 AAB = versionCode 1 = 웹 `v41`**(2026-07-21 게시, 심사 통과). 그 뒤 웹에만 반영한 것:
- **`v42`~`v45` 이 앱 공유하기**(2026-07-22) — 안드로이드=Play·아이폰=웹 두 링크. 공유는 3곳:
  타이틀(출발 버튼 아래), 엔딩(버튼 둘 아래), 지도 상단바(모드 버튼 왼쪽 `🔗 알려주기` 알약).
  `v45` 에서 타이틀·엔딩은 **버튼 대신 작은 민트 글자 링크**(`.share-link`)로 — 공유는 부차적 동작이라
  큰 컬러 버튼과 안 겹치게. 지도만 알약 유지(툴바라 글자는 어색).
  ⚠️ 안드로이드(Play) 공유 링크는 앱이 **공개 출시돼야** 열림(비공개 테스트 중엔 404).

**옮기는 절차** (12명×14일 채운 뒤, 한 번에):
1. `cd sarangnara-app && npm run sync`  ← 웹 원본을 `www/` 로 복사
2. 안드로이드 `versionCode` 1→2, `versionName` 도 올림 (`sarangnara-app/릴리스-빌드-절차.md`)
3. `npm run release` → 서명 AAB 빌드
4. Play Console 비공개 테스트 트랙 → **`버전 수정`** 으로 들어가 새 AAB 업로드 → 검토 제출
   (트랙에 **빈 임시 버전이 하나 있다**. 그것 때문에 `새 버전 만들기` 가 회색으로 잠겨 있고
   `버전 수정` 만 열린다. 그 초안에 AAB 를 얹으면 그대로 versionCode 2 가 되니 **따로 지울 것 없다.**
   내용은 `국가/지역 1` 한 줄뿐이고 검토 대기열에도 없어서 지금 돌아가는 테스트엔 영향 없음. 2026-07-24 확인)
- 🚫 **12명×14일 도는 동안에는 새 AAB 를 올리지 말 것** — 심사 리셋 걱정은 이제 없지만(승인 완료),
  프로덕션 액세스 심사가 걸린 기간이라 움직이는 부품을 늘리지 않는다. 웹 변경은 이미 웹에 살아 있고
  Play 공유 링크는 어차피 공개 출시 전엔 안 열려서 서두를 실익도 없다.
- (스토어 자산) 태블릿 스크린샷 16:9 4장도 아직 콘솔 미업로드 — `sarangnara-app/store-assets/tablet/`.

## 2026-07-18~19 완료한 것
1. ✅ git 초기화 + GitHub(SYA-Apps/sarangnara) 배포 + Pages 활성화
2. ✅ **글씨체 깨짐 수정** — Gaegu·Gothic A1 폰트를 흔한 한글 전체로 재-subset,
   인라인 base64 → `fonts/*.woff2` 외부 파일로 분리 (index.html 447KB→약 128KB)
3. ✅ **인트로/엔딩 배경음악** — 이미 작곡돼 있던 BGM이 트리거 안 되던 것 수정,
   볼륨 상향(화음 .04 / 멜로디 .075), 첫 터치에 재생 + 오디오 깨어나는 즉시 재시도(v9)
4. ✅ **회전 시 크래시 수정** — renderSystem 에서 곡 끝난 뒤 si 범위 벗어나면 방어
5. ✅ **8분음표 깃발 안 보이던 버그** — 지도 마커용 `.flag` CSS 와 음표 깃발 클래스
   충돌 → 음표 깃발을 `.nflag` 로 분리
6. ✅ **비행기 리듬** — 사용자 악보대로 교정 (떴~다 점4분+8분, 소절끝 2분음표, 끝 온음표)
7. ✅ **등대지기 · 즐거운 나의 집** — 사용자가 zip으로 준 완성 악보 데이터로 교체
   (비행기·폰트·기타 수정본은 유지하고 이 두 곡 notes 만 교체)
8. ✅ **iOS 사파리 더블탭 확대 방지** — 빠르게 칠 때 화면 확대되던 것,
   350ms 이내 연속 touchend 확대동작 취소 + gesturestart 등 핀치 확대 차단 (버튼은 예외)

## 2026-07-19 완료 (이어서)
9. ✅ **서비스워커 network-first 개선(v11)** — index.html·manifest 같은 페이지는 네트워크
   우선으로 받고, 안 되면 캐시 폴백(오프라인 유지). 폰트·아이콘은 캐시 우선 그대로.
   → 앞으로는 **새로고침 한 번이면 최신 버전**이 뜸.

10. ✅ **패드 글씨 크기**(v13) — 타이틀에 큰 화면용 규칙이 없어 폰 크기 그대로였음.
    min-height 620/800 두 단계로 확대. 폰 가로에서 '출발!' 버튼 잘리던 것도 함께 수정.
11. ✅ **곡 박자 교정**(v14~15) — 등대지기 여 96→110·남 114→124, 즐거운 나의 집 남 124→110.
    중복돼 있던 계산식을 `applyTempos()` 하나로 합치고 곡별 예외는 `TEMPO_FIX` 표로 분리.
12. ✅ **아이콘 교체**(v16) — 건반 7→5개, 피아노 크게. PNG 뿐이던 아이콘을 **벡터(SVG)로 복원**해
    `sarangnara-app/make-icon.mjs` 로 어떤 크기든 생성 가능(애플용 1024 포함).
13. ✅ **나비야 전곡**(v17·v19) — 8마디 → 악보 그대로 16마디, 마디 끝 쉼표를 2박 음표로 교정.
14. ✅ **옛 웹뷰에서 게임이 아예 안 돌던 문제**(v18·v20) — 갤럭시 S8(웹뷰 72) 실측으로 발견.
    자바스크립트가 **문법 오류로 한 줄도 실행되지 않고** 있었음(보이던 건 전부 정적 HTML).
    - `??`(널 병합), `?.`(옵셔널 체이닝) → 크롬 80+ 문법이라 파싱 단계에서 전멸
    - CSS `inset:0` → 크롬 87+ 라 무시돼 `.screen` 이 303x42 로 쪼그라듦(지도·연주 화면 안 보임)
    - SVG 요소의 `innerHTML` → 크롬 86+ 라 무시됨 → `setSVGHTML()` 로 12곳 교체
15. ✅ **앱에서 인트로 음악 자동재생**(v21) — 브라우저는 첫 터치 규칙이 있어 웹은 그대로,
    앱만 `setMediaPlaybackRequiresUserGesture(false)` 로 켜자마자 재생.
16. ✅ **안드로이드 전체화면 + 노치 흰 띠 제거** — 시스템 바 숨김, 웹뷰가 안 칠하는 노치 영역을
    게임 배경색으로. (캐패시터는 기기 웹뷰가 140 미만이면 부모 뷰에 패딩을 넣는다)

## 스토어 출시 준비 (Google Play → 추후 App Store)

### 확정된 사항 (바꾸지 말 것)
- **패키지 ID: `com.sya.sarangnara`** — 출시 후 영구 변경 불가. 양 스토어 동일하게 사용.
- **Google Play 개발자 계정: 개인 계정** — 정식 출시 전 *비공개 테스터 12명이 14일 연속 테스트*
  요건 통과 필요. 이게 가장 오래 걸리므로 일찍 시작할 것.
- **패키징 방식: Capacitor** (TWA 아님) — 웹 파일을 앱 안에 **동봉**하는 방식.
  이유: 애플은 원격 주소만 여는 웹 래퍼를 심사 4.2(최소 기능)로 거절하는 경우가 많음.
  처음부터 Capacitor로 하면 안드로이드·iOS를 같은 프로젝트에서 뽑아 재작업이 없음.
  웹 버전(GitHub Pages)은 그대로 계속 유지.

### 현재 앱 상태 (심사 관점 — 이 상태를 깨지 말 것)
- 외부 네트워크 요청 **0건** (폰트도 로컬 파일)
- 광고·애널리틱스·third-party SDK **없음**
- 개인정보 수집 **없음** — localStorage에 캐릭터 선택(girl/boy)·진행도·점수·시각만 기기 저장
- 로그인·계정·외부 링크·앱내결제 **없음**
- ⚠️ 광고나 애널리틱스를 한 줄이라도 넣으면 아동용 심사 난이도가 급격히 올라감

### 완료
- [x] **Capacitor 앱 셸 구성** — `C:\SYA\Sarangnara\sarangnara-app` (별도 git repo)
      - `npm run sync` → 웹 원본을 `www/` 로 복사. **웹이 항상 원본**, 곡 고칠 땐 sarangnara-web만 고침
      - `npm run build` → sync + cap sync / `npm run open` → 안드로이드 스튜디오
      - **인터넷 권한 0개**(완전 오프라인 → 데이터 안전 신고가 단순해짐)
      - 화면 방향은 고정하지 않음(세로로 들면 게임이 '가로로 돌려주세요'를 띄움)
      - targetSdk 36, 어댑티브 런처 아이콘 생성 완료
      - 디버그 APK 빌드 성공 확인 (JAVA_HOME = 안드로이드 스튜디오 번들 JDK)
      - index.html: Capacitor 안에서는 서비스워커 등록 안 하도록 가드 추가 (v12)
- [x] 개인정보처리방침 페이지 — `privacy.html` (한/영), 공개 URL:
      https://sya-apps.github.io/sarangnara/privacy.html  ← 양 스토어 제출 시 이 주소 사용

- [x] **업로드 키 생성 + keystore.properties** — `android/sarangnara-upload.jks`
      (git 에 안 올라감. 구글 드라이브에 백업 완료. 절차는 `릴리스-빌드-절차.md`)
- [x] **서명된 AAB 빌드 확인** — `npm run release` 한 줄. jarsigner 검증 통과
- [x] **스토어 아트웍·문안** — `store-assets/` (폰·태블릿 각 4장, 피처 그래픽, 등록정보.md)

### 남은 준비
- [ ] **Google Play 개발자 계정 승인 대기 중** (등록·결제는 완료)
- [ ] 승인되면: 앱 만들기 → 비공개 테스트에 AAB 업로드 → **Play 앱 서명 켜기**
- [x] 비공개 테스터 모으기 — **14명 확보**(2026-07-22, 요건 최소 12명 → 2명 여유) → 이제 14일 연속 테스트 유지
- [ ] 콘텐츠 등급 설문(IARC), 데이터 안전 양식("수집 안 함"), 타겟 연령 설정, Families 정책 검토
- [ ] (추후 애플) $99/년, 아동 카테고리 심사, 개인정보 라벨, iPhone·iPad 스크린샷.
      **iOS 빌드는 맥 + Xcode 필요** — 윈도우에서는 불가

## 2026-07-19~20 완료 (이어서)
17. ✅ **지도에 곡 힌트 그림**(v22·v24) — 열 사람 옆에 그 곡을 떠올리게 하는 그림.
    비행기→종이비행기, 우리집에왜왔니→우체통, 나비야→나비, 똑같아요→**새 두 마리**,
    작은별→별, 퐁당퐁당→물결, 봄→**꽃밭**, 등대지기→등대, 생일→케이크, 즐거운나의집→굴뚝 연기.
    도와주기 전 회색 → 도와주면 색 + 살짝 움직임. 자리는 `PROP_POS` 로 사람마다 지정.
    쓰레기도 자리마다 다르게 흩고 기울기·크기 제각각 + 바닥 얼룩(집 쪽으로 모음).
18. ✅ **박자 전면 재조정**(v23·v29·v30) — 계산식을 버리고 곡별 표 `TEMPO` 로 직접 지정.
    기준을 '부르는 속도' → '아이가 칠 수 있는 속도'로. 8분음표 있는 곡은 더 낮춤.
    등대지기는 음표가 0.77박마다 나와 가장 촘촘해서(다른 곡 1.0~1.35) 별도로 더 낮춤.
    **6/8 표시가 ♪ 로 잘못돼 있던 것도 수정** — 데이터의 1박은 4분음표라 ♩.= 로 환산해 표시.
19. ✅ **나비야 16마디 전곡**(v19), **봄 점2분음표→2분음표+쉼표**(v26), 제목 `봄(비발디)`
20. ✅ **아이콘 교체**(v16) — 건반 7→5개, 피아노 크게. 벡터(SVG) 원본 확보로 어떤 크기든 생성
21. ✅ **끝내도 사라지지 않게 + 놀이 방식 선택**(v28·v30~v34)
    - 엔딩에 '사랑나라 더 놀기' — 색 돌아온 마을 그대로 두고 계속 논다
    - 한 번 깨면 **순서대로 / 마음대로** 를 고를 수 있다(엄마는 계속 마지막)
    - 타이틀은 캐릭터 양옆에, 지도는 상단바 버튼으로 — 양쪽이 같은 값을 쓴다
    - 지도에 **'처음 화면'** 버튼 (진행상황 저장하고 타이틀로)
22. ✅ **카운트다운 1초 고정**(v32) — 곡 박자와 무관하게 모든 곡 4·3·2·1 을 1초씩.
    `R.leadMs` 를 따로 두고 음표 시각 계산을 전부 그 기준으로. 만점 판정으로 검증.
23. ✅ **전체화면 버튼 숨김**(v25) — 앱·PWA·아이폰 사파리에서는 눌러도 소용없어 숨김
24. ✅ **사파리 확대 방지 보강**(v27) — 두 손가락 동시 터치를 핀치로 오해하던 것 차단
25. ✅ **옛 웹뷰 글씨 겹침**(v27) — flex `gap` 미지원 감지해 margin 으로 대체

## 2026-07-20 완료 (이어서)
26. ✅ **갤럭시 S8 실측 점검** — 디버그 APK 새로 빌드·설치 후 `walk-phone.mjs` 로
    타이틀 → 지도 → 대화 → 연주까지 실제로 눌러가며 확인. **자바스크립트 오류 0건**,
    악보 65개 요소 정상 그려짐, 화면 밖으로 새는 요소 없음. 옛 웹뷰 문제는 다 잡힌 상태.
27. ✅ **악보 카드가 안내 문구를 덮던 것**(v35) — 세로가 빠듯한 가로 화면에서
    악보 카드(114px)가 배정된 자리(96px)보다 커져 아래 "빛나는 음표를 보고…" 를 9px 덮었다.
    **S8뿐 아니라 아이폰 가로(844x390)도 8px 겹쳤음.**
    - 원인: `svg.staff{max-height:100%}` 는 부모 높이가 auto 라 안 먹는다.
    - 고침: `fitStaff()` 가 '남은 높이'와 '제 비율 높이' 중 작은 쪽을 직접 넣는다.
      비율은 viewBox 에서 읽으므로 **두 줄짜리 악보(620x192)도 맞는다**.
    - 넓은 화면(패드)은 비율 쪽이 작아 전과 똑같음 — 카드가 통째로 늘어나지 않는다.

28. ✅ **처음부터 끝까지 자동 플레이 점검** — `playthrough.mjs`(헤드리스),
    `playthrough-phone.mjs`(실제 S8). 음표가 나올 때마다 그 건반을 실제로 눌렀다 떼므로
    판정·홀드·줄 넘김·엔딩까지 실제 플레이와 같은 길을 지난다.
    - 헤드리스 6판(S8·패드 × 연습·박자·전곡) + 폰 3판 = **9판 모두 10/10 완주, 엔딩 정상, 오류 0건**
29. ✅ **쉼표 때문에 정확도가 100% 가 안 나오던 것**(v36) — 위 점검에서 발견.
    분모 `flatCount` 는 쉼표까지 세는데 판정 대상 `sysNotes` 는 칠 음만이라,
    **한 음도 안 틀려도 쉼표 개수만큼 깎였다.** 우리집에 왜 왔니는 MISS 0 인데 81%·B등급.
    **10곡 중 6곡이 완벽하게 쳐도 S 를 못 받는 상태였다.**
    채점 분모를 `playCount`(칠 음만) 로 분리 — 진행바·전리품 눈금은 `flatCount` 그대로라 안 흔들린다.
    → 자동 연주로 10곡 전부 S · 100% 확인.

30. ✅ **앱 안 개인정보처리방침 + 오픈소스 라이선스**(v37) — **정책 위반 상태였다.**
    구글: "All apps must post a privacy policy link in the designated field within Play Console,
    **and a privacy policy link or text within the app itself**." 스토어 등록란만으론 부족한데,
    `sync-web.mjs` 복사 목록에 `privacy.html` 이 없어 앱 안엔 아예 들어가 있지 않았다.
    - `privacy.html` 을 앱에 동봉 + `licenses.html` 새로 작성
    - 타이틀 맨 아래 작은 회색 글씨로 `개인정보처리방침 · 만든 것들` 한 줄
    - 폰트 둘 다 **SIL OFL 1.1** 이라 저작권 고지+전문 포함이 의무:
      Gaegu(Copyright 2018 The Gaegu Project Authors), Gothic A1(Copyright HanYang I&C Co., Ltd.)
      Capacitor(MIT)·AndroidX(Apache 2.0) 전문도 함께 실었다.
    - **인터넷 권한 0개는 그대로** — 앱에 함께 담긴 로컬 파일이라 바깥으로 나가지 않는다
    - 두 페이지에 `← 사랑나라로 돌아가기` 링크 (앱에선 나올 방법이 없으면 갇힌다)
    - `check-legal.mjs` 로 헤드리스 검증 + 실제 S8 웹뷰에서도 열림·복귀 확인

## 2026-07-20 완료 (이어서)
31. ✅ **연락처를 개발자 계정으로 통일**(v38) — 개인정보처리방침 문의 주소를
    `sya.apps.dev@gmail.com` 으로. 나란히와 같은 편지함을 쓰므로 mailto 제목에
    `[사랑나라]` 를 미리 넣어 섞여 들어와도 가려지게 함. 영문 문의는 영문 제목으로(v39).
32. ✅ **악보 폭·개인정보 링크**(v40) — 악보가 다시 화면 폭을 꽉 채우게, 개인정보 링크는 구석으로.
33. ✅ **마음대로 모드 지도 클릭 버그**(v41) — 마음대로 모드인데 지도에서 사람이 안 눌리던 것 수정.

## 2026-07-22 완료
34. ✅ **이 앱 공유하기**(v42~v45) — 공유 3곳(다 `shareApp` 하나로): 타이틀·엔딩·지도 상단바.
    타이틀·엔딩은 **작은 민트 글자 링크**(`.share-link`, 손글씨체, `#shareTop`/`#endShareBtn`),
    지도는 알약(`#shareMap`, `.tbtn`, 모드 버튼 왼쪽). 크기·형태 변천: v43 큰 버튼 → v44 작은 버튼 →
    v45 글자 링크(부차 동작이라 큰 컬러 버튼과 안 겹치게). 세 요소 다 `<a role=button>`/`.tbtn`.
35. ✅ **링크 공유 카드 이미지(OG)**(v46) — 카톡 등에서 웹 링크를 공유하면 썸네일 없이 기본 문구만
    뜨던 것 수정. `index.html` head 에 Open Graph/Twitter 메타태그 + **`og-image.png`(1200×630)** 추가.
    이미지는 `sarangnara-app/make-og.mjs` 로 생성(스토어 피처그래픽과 같은 결, 아이콘+제목+소개+건반).
    - og:image 는 **절대 URL**(`https://sya-apps.github.io/sarangnara/og-image.png`) 이어야 크롤러가 읽는다.
    - OG 태그·이미지는 크롤러만 가져가고 앱은 안 부르므로 **'외부 요청 0건' 유지**. og-image 는 app `www/` 미포함.
    - ⚠️ 카톡은 OG 를 세게 캐시한다. 새 카드가 바로 안 뜨면 카카오 캐시 초기화(`developers.kakao.com/tool/clear/og`)
      로 재수집하거나 하루쯤 기다린다. 공유 문구의 첫 링크(안드로이드=Play)는 출시 전엔 카드가 없어,
      지금은 웹 링크 카드가 주로 뜬다. 누르면 OS 공유 시트(`navigator.share`) → 없으면 클립보드 복사(execCommand
    폴백, 옛 웹뷰까지) → 그것도 안 되면 링크 토스트. **인터넷 권한 0개 유지**(OS 에 넘길 뿐 앱이
    직접 네트워크 안 씀). 받는 사람 기기에 맞게 **두 링크로 나눠** 보냄:
    안드로이드=Play 스토어(`shareApp` 의 `SHARE_URL_AOS`), 아이폰·기타=웹(`SHARE_URL_WEB`).
    ⚠️ **안드로이드(Play) 링크는 앱이 '공개' 출시돼야 열린다**(비공개 테스트 중엔 404). 웹은 항상 열림.
    `navigator.share` 취소(AbortError)는 반드시 `.catch` — 안 그러면 전역 unhandledrejection 이 오류바를 띄운다.
    - **웹에는 배포하되 앱(AAB)에는 아직 안 넣음**: Play 링크는 어차피 공개 출시 전엔 안 열린다.
      14일 테스트를 채우고 출시 즈음 versionCode 2 로 묶어 반영.
      (당시엔 "심사 리셋" 이 이유였는데 2026-07-24 승인 확인으로 그 이유는 사라졌다. 위 '옮기는 절차' 참고)

## 2026-07-24 — 폰에서 악보가 안 보이던 버그 수정 (v47)
36. ✅ **폰 가로에서 악보가 안 보임/노래만 나옴**(v47) — 테스터 신고. 원인 두 가지:
    - **(squeeze)** 세로가 짧은 폰 가로에서 악보 칸(`.score-wrap`)이 `min-height:0` 이라 0 까지 눌리고,
      건반(`.kbv min-height:124`)이 자리를 다 먹어 악보가 **읽을 수 없는 두께로 쪼그라들었다**.
      iframe(vh=폰높이)로 재현·측정: 현재 코드는 세로 300→악보 53px, 280→33px, **256→13px**.
    - **(race)** 진입 때 `show('screenPlay')` 직후 rAF 한 프레임 뒤 `fitStaff` 가 도는데, 느린 웹뷰(갤S)에서
      그 순간 화면이 아직 안 떠 `avail<=0` → `fitStaff` 가 그냥 return → svg 가 height:auto(제 비율)로
      남아 좁은 칸을 넘치고 화면 `overflow:hidden` 에 **잘려 통째로 안 보였다**(회전 전엔 안 고쳐짐).
    - 고친 것: ① `.score-wrap{min-height:92px}`(짧은 화면 84) 로 악보 칸 바닥 보장,
      ② 짧은 화면 미디어쿼리(`max-height:430/360`)에서 **건반을 살짝 낮춰**(min 124→104→88, 손가락엔 충분)
      악보 자리를 냄, ③ `fitStaff` 가 avail 이 작아도 항상 '남은 높이'로 눌러 칸 안에 넣게(잘림 방지),
      ④ `settleStaff()` — 진입 뒤 몇 프레임·`document.fonts.ready` 후 `fitStaff` 재실행(느린 기기 race 방지).
    - TO-BE 측정(같은 iframe): 세로 300→악보 88px, 280→76px, **256→76px**(건반 88, 겹침·잘림 없음). 읽을 수준 유지.
    - ⚠️ 렌더링 코드(fitStaff 등)는 v41 이후 안 바뀌었어서, 이 버그는 **테스터가 쓰는 앱(v41 동봉본)에도 그대로 있다.**
      웹 push 로는 앱에 안 닿는다 → **versionCode 2 AAB 를 올려야 테스터에게 반영.** (심사 리셋 걱정 없음)
    - ✋ **올리기(AAB) 전에 웹 + 갤럭시S 실물에서 동시에 눈으로 확인**하기로 함(2026-07-24).
    - ✅ 갤S8 웹(가로)·아이폰 사파리에서 악보 정상 확인(2026-07-24, 테스터).
37. ✅ **엔딩 버튼 크기 통일**(v48) — 엔딩의 `사랑나라 더 놀기`(핑크)·`처음부터 다시`(민트) 버튼이
    글자 수가 달라 폭이 달랐다(측정: 184 vs 159px). `.end-btns .go-btn` 에 `min-width:9.6em`+`nowrap`+
    `flex:0 0 auto` 로 **둘 다 192×37px 동일**하게. 긴 글자도 한 줄 유지, 아주 좁으면 flex-wrap 로 위아래 감쌈.
    (두 버튼 다 인라인 `font-size:20px;padding:6px 26px` 로 높이는 원래 같았고 폭만 문제였다.)

## 2026-08-03 — 폰 가로에서 '사랑나라로' 버튼이 화면 밖으로 나가던 버그 (v49)
38. ✅ **연주 화면 상단바가 화면 밖으로 샘**(v49) — 갤S8 실물(versionCode 2)에서 눈으로 발견.
    곡 이름이 길면 상단바가 통째로 밀려 **오른쪽 끝 `사랑나라로`(지도 복귀) 버튼이 잘렸다.**
    - 원인: `.bar` 는 `flex-wrap:nowrap` 인데 `.tag`(제목)에 **`min-width:0` 이 없었다.**
      flex 항목의 기본 `min-width:auto` 는 제 내용보다 작아지는 걸 막아서, 자리가 모자라도
      **아무것도 안 줄고 그대로 넘친다.** 박자·전곡 모드는 `SCORE` 칸이 더 붙어 30px 쯤 더 나쁘다.
    - AS-IS 측정(S8 가로 = CSS 740x360): 우리집에 왜 왔니 26px, 등대지기 8px,
      생일 축하합니다 65px, **즐거운 나의 집 99px** 넘침.
      **즐거운 나의 집은 버튼이 통째로 화면 밖**(left=773 > 화면폭 740)이라 아예 못 누른다.
      🚨 **탈출구가 없다** — 코드에 back 처리가 없어서 **안드로이드 뒤로가기를 누르면 앱이 그냥 종료된다**
      (실제로 눌러 확인). 아이가 그 곡에 들어가면 앱을 끄는 것 말고는 지도로 못 돌아간다.
    - 고침: ① `.tag{min-width:0;overflow:hidden;text-overflow:ellipsis}` — 제목만 줄고
      버튼은 제 크기를 지킨다. **어떤 곡 이름이 와도 밖으로 안 나가는 안전망.**
      ② 폰 가로(`max-height:430`)에서만 `.meter`(박자·빠르기)를 숨기고 `.bar .tbtn`·`.oct-pill`
      좌우 여백을 줄여, 실제로는 제목이 안 잘리게 자리를 냈다. 박자표는 악보에 그려져 있어 없어도 된다.
    - TO-BE: 10곡 × 3모드 × (아이패드·아이폰·S8) **90조합 전부 여백 12px, 넘침 0, 말줄임 0.**
      640x320 까지 좁혀도 넘침 0(제목만 말줄임).
    - ✅ **갤S8 실물 크롬 72 에서 재확인** — 브라우저 UI 때문에 앱보다 더 빠듯한 **692x280** 인데도
      30조합 전부 넘침 0·말줄임 0·여백 12px. `nogap` 폴백이 켜진 상태로 잰 값이다.
    - ⚠️ **이 버그는 테스터가 쓰는 versionCode 2 에 그대로 들어가 있다.** 다만 뒤 곡은 앞사람을 다
      도와야 열려서 아직 아무도 거기까지 안 갔을 수 있다. → versionCode 3 으로 반영.
    - 📌 **뒤로가기 처리는 아직 없다.** 이번엔 버튼이 안 잘리게만 고쳤다.
      `@capacitor/app` 의 `backButton` 리스너로 지도 복귀를 붙이는 건 남은 일(아래 '다음 확인/할 일').

## 2026-07-21 — 계정 블로커 해소
- ✅ **Google Play Console 본인 인증 완료** — 반려됐던 주소지 증빙 재제출건이 통과.
  관리자 메일함(`sya.apps.dev`)에 "본인 인증이 완료되었습니다"(2026-07-20 도착) 확인.
  이제 앱 만들기 → 비공개 테스트 업로드가 가능한 상태.
- ✅ **Google Payments 본인확인**(uid 3·10, 결제 프로필) — 개발자가 완료했다고 확인(2026-07-22). 더 볼 것 없음.

## 2026-07-21 — Play Console 비공개 테스트 제출 완료 🎉
- ✅ **앱 생성** — `com.sya.sarangnara`, 스토어 앱 이름 `사랑나라 - 피아노 동요`(홈 화면은 `사랑나라`).
- ✅ **AAB 업로드 + Play 앱 서명 켜짐** — v41 서명 AAB(versionCode 1). 비공개 테스트 트랙 `사랑나라_260721`.
- ✅ **앱 콘텐츠 선언 10개 전부** — 개인정보처리방침(URL 정확)·콘텐츠등급(전체이용가)·데이터안전(수집 안 함)·
      타겟층(만 13세 미만: 5이하+6\~8+9\~12 → Families 정책)·광고 없음·광고ID 없음·앱액세스·정부앱 아니오·금융 없음·건강 없음.
- ✅ **스토어 등록정보** — 간단/자세한 설명, 앱 아이콘(512), 그래픽 이미지(피처그래픽), **휴대전화 스크린샷 4장**.
      ⚠️ 태블릿 스크린샷은 우리 이미지가 **16:10(2048x1280)**이라 요구된 **16:9**와 안 맞아 **뺐음**(저장·제출엔 지장 없음).
      ⚠️ 피처그래픽 1024x500은 picker 가 "너무 작다"고 거부 → **2048x1000 으로 2배 키워 '그래픽 이미지' 크롭 프리셋으로** 넣음.
- ✅ **스토어 설정** — 카테고리 **음악(게임)**, 연락처 이메일 `sya.apps.dev@gmail.com`(공개됨, 개인메일 금지 규칙).
- ✅ **테스터 이메일 목록 `사랑나라 테스터`** 만듦 — 처음 **3명**으로 시작(2026-07-22 에 14명으로 늘림).
      🚫 **명단(이름·이메일)은 이 파일에 적지 않는다. 이 파일은 공개 저장소에 올라간다.**
      명단 원본은 **Play Console 의 테스터 목록**이고, 인원·경과일 추적은
      `C:\SYA\tools\테스터-관리표.html`(git 저장소 아님)에서 한다.
      (2026-07-24: 처음엔 "콘솔 테스터 목록은 비공개니 적어도 된다"고 판단해 주소를 적었는데,
      **적는 장소가 공개**라는 걸 놓친 것이었다. 히스토리에서까지 지우고 이 규칙으로 바꿨다.)
- ✅ **검토 제출** — 게시 개요에서 "변경사항 14개 제출" 완료. 상태 = **"검토 중인 변경사항"**.
      **함정**: 릴리스가 "임시"일 때 *버전 미리보기 및 확인 → 저장*을 안 하면 전송 버튼이 잠긴다. 그거 저장 후 제출됨.
- 심사: ~~보통 7일 이내. 결과는 `sya.apps.dev` 메일로.~~
      → **같은 날 오후 5:04 에 바로 게시됐고 메일은 오지 않았다**(2026-07-24 확인). 아래 07-24 항목 참고.

## 2026-07-24 — 심사 통과·게시 확인 (메일은 안 왔다)
- ✅ **비공개 테스트 릴리스 `1 (1.0)` 게시 완료** — 트랙 `사랑나라_260721` 에서
      "**선택한 테스터에게 제공됩니다**", **게시일 7월 21일 오후 5:04**.
      게시 개요 = "게시되지 않은 변경사항이 없습니다". 대시보드의 프로덕션 액세스 조건에서도
      "비공개 테스트 버전 게시" 가 취소선으로 완료 표시됨. **제출 당일 바로 통과한 것.**
- ⚠️ **승인 메일은 오지 않는다** — 관리자함(`sya.apps.dev`) 30일치를 스팸함까지 훑어도
      Play 심사 결과 메일이 없다. 비공개 테스트 첫 게시는 Play 가 따로 메일을 안 보내는 듯.
      **메일만 기다리면 통과한 줄 모르고 계속 기다리게 된다 — 콘솔이 사실이다.**
- 🚨 **옵트인이 1명뿐이었음** — 테스터 목록 `사랑나라 테스터` 에는 **14명**이 등록돼 있지만
      실제 참여를 선택한 사람은 **1명**, 설치한 사용자 **0명**이었다.
      대시보드 문구: "12명 이상의 테스터가 비공개 테스트 참여 선택 — *현재 참여를 선택한 테스터 N명*".
      **14일 카운트는 12명이 옵트인한 날부터 시작한다. 목록에 이메일만 넣은 건 카운트에 안 잡힌다.**
      → 같은 날 테스터들에게 안내문 발송, **몇 시간 만에 1명 → 5명**. 남은 건 7명.
      **옵트인 수는 이 대시보드에서만 보인다**(테스터 목록의 "14명"은 등록 인원이라 계속 14로 보임).
- 테스터가 실제로 해야 하는 것 (둘 다 해야 함):
      1. `https://play.google.com/apps/testing/com.sya.sarangnara` 에서 **"테스터 되기"** 누르기
      2. 같은 페이지의 Play 스토어 링크로 **설치까지** 하기
      → **목록에 등록된 그 계정으로 로그인한 상태**여야 한다. 옵트인만 하고 안 깔면 의미 없음.
- 도구: `check_admin_mail.py` 의 `--search` 가 한글이면 `UnicodeEncodeError` 로 죽던 것 고침.
      (IMAP 리터럴 + `CHARSET UTF-8`. 이제 `--search 심사` 가 된다.)
- 참고: 콘솔 "다음 출시 버전을 위한 발견 항목" 에 **Android 15 edge-to-edge** 경고가 떠 있다.
      versionCode 2 만들 때 볼 것(`enableEdgeToEdge()` / `EdgeToEdge.enable()`).

## 2026-08-18 — 뒤로가기 실기기 검증 완료 ✅
- ✅ **versionCode 4(1.3)가 Play로 갱신된 것 확인**(`adb dumpsys package`, lastUpdateTime 08-17 21:28).
- ✅ 릴리스 빌드라 원격 디버깅은 안 되므로(전에 확인한 대로) `adb shell input keyevent`로
  직접 눌러가며 확인. 세 갈래 전부 실제 기기(갤S8)에서 통과:
  - 연주 화면 → 뒤로가기 → **지도로 복귀** (앱 안 죽음)
  - 지도 → 뒤로가기 → **타이틀로 복귀** (앱 안 죽음)
  - 타이틀 → 뒤로가기 → **앱 정상 종료**(`dumpsys activity`로 포그라운드가 런처로 바뀐 것 확인)
- 폰 화면 항상 켜짐 요청(`screen_off_timeout`)·강제 가로 회전(`user_rotation`)은 테스트용으로
  건드렸다가 끝나고 원복(`accelerometer_rotation 1`)함.
- **연주 화면에서 뒤로가기로 앱이 그냥 종료되던 문제, 완전히 해결됨.**

## 2026-08-17 (이어서) — versionCode 4(1.3, 뒤로가기) 업로드, 실기기 검증은 대기
- ⚠️ **테스트용 갤S8이 옵트인 12명 중 하나(Play 설치, versionCode 2, `com.android.vending`)**라는
  걸 `adb dumpsys package` 로 확인 — 여기 디버그 APK를 깔면 재설치로 카운트가 깨질 위험.
  뒤로가기는 네이티브 Capacitor 플러그인이라 폰 크롬 우회도 안 통함(웹뷰엔 플러그인이 없음).
- ✅ 그래서 versionCode 4(1.3, v50 뒤로가기 반영)를 **정식 서명 릴리스로 빌드**
  (`npm run release`, `jarsigner -verify` → jar verified) → Play Console `사랑나라_260721`
  트랙에 업로드 → 어제 미제출 상태였던 의견 URL 변경사항과 함께 검토 제출.
  상태 = "검토 중인 변경사항". **게시되고 이 폰이 Play로 업데이트를 받아야 실기기 검증 가능** —
  다음 확인 때 `adb shell dumpsys package com.sya.sarangnara`로 versionCode=4 인지 먼저 보고,
  그다음 연주→뒤로가기(지도로)·지도→뒤로가기(타이틀로)·타이틀→뒤로가기(종료) 셋 다 확인할 것.

## 2026-08-17 — 재신청 답안 Q1·Q2 실제 근거로 채움
- ✅ `production-access-application.md` Q1(모집 경로: 가족·지인·직장 동료, 카톡·문자
  개별 연락)·Q2(v47 신고 경로: 카톡·문자, 이게 테스트 기간 유일한 피드백) 사용자 확인 완료.
- ✅ versionCode 3(1.2) 게시 확인(콘솔 "최신 출시 버전: 3 (1.2)", 게시일 8월 9일 오전 10:26) —
  Q3에 3번 문단(버튼 잘림 수정) 반영.
- 옵트인은 여전히 정확히 12명(여유 0) — 대시보드가 8/16·8/17 확인 둘 다 "2일 동안 참여"로
  표시돼 정확한 재시작일은 아직 불명확. 손으로 세지 말고 콘솔 초록불로 판단할 것.

## 2026-08-16 — 뒤로가기 처리 (v50 / versionCode 4)
39. ✅ **연주 화면 안드로이드 뒤로가기 처리** — `@capacitor/app`(`App.addListener('backButton', …)`)로
    연주중 뒤로가기→`backBtn` 과 동일(지도로), 지도에서 뒤로가기→`homeBtn` 과 동일(타이틀로),
    타이틀에서만 `App.exitApp()`. 기존 버튼 로직을 그대로 재사용(`.click()`), 새 로직 중복 없음.
    리스너는 `window.Capacitor.Plugins.App` 이 있을 때만 등록 — 웹에는 영향 없음.
    `npx cap sync android` 로 네이티브 등록 확인(`Found 1 Capacitor plugin for android: @capacitor/app@8.1.1`),
    `gradlew assembleDebug` 컴파일 성공. **⚠️ 실기기 검증은 아직 안 함**(연결된 폰 없음) —
    다음 세션에서 갤S8·에뮬레이터로 연주→뒤로가기, 지도→뒤로가기, 타이틀→뒤로가기(종료) 셋 다 확인할 것.
    versionCode 4 / versionName 1.3, 웹 v50.

## 2026-08-18 — 프로덕션 액세스 재신청 진행상황 콘솔 확인
- ✅ Play Console 대시보드(`사랑나라 - 피아노 동요` → 대시보드 → 프로덕션)에서 직접 확인:
  > "검토일부터 12명 이상의 테스터를 대상으로 14일 더 비공개 테스트 실행 —
  > 현재 4일 동안 참여를 선택한 테스터 12명" (검토 시점: "금요일, 오후 1:47" 표시)
- **옵트인 인원 = 정확히 12명, 여유 0명** — 전과 동일하게 위험 구간. 한 명만 빠져도 리셋.
- **진행 일수 = 오늘(8/18) 기준 4일째** → 역산하면 재시작일 ≈ 2026-08-15 무렵(콘솔이 요일로만
  표시해 정확한 날짜는 안 나옴). 앞으로 **10일 더 필요 → 재신청 가능 예상일 ≈ 2026-08-28**,
  `production-access-application.md` §1 의 어림짐작과 일치.
- "프로덕션 신청" 버튼은 아직 비활성(회색) — 요건 미충족 중.

## 2026-08-24 — 옵트인 진행상황 확인 + 박자 최종 확인 완료
- ✅ Play Console 대시보드 직접 확인: "검토일부터 12명 이상의 테스터를 대상으로 14일 더
  비공개 테스트 실행 — 현재 **10일** 동안 참여를 선택한 테스터 **12명**" (검토 시점: 8월 14일 오후 1:47).
  **여전히 정확히 12명, 이탈 없음.** 4일만 더 유지되면 14일 충족 →
  **프로덕션 액세스 신청 가능일 ≈ 2026-08-28** (기존 어림짐작과 일치).
- ✅ **아이 실제 플레이 박자 최종 확인 완료** — 등대지기·나비야 포함 문제 없음.

## 2026-08-26 — 옵트인 진행상황 재확인
- ✅ Play Console 대시보드 확인: "검토일부터 12명 이상의 테스터를 대상으로 14일 더
  비공개 테스트 실행 — 현재 **12일** 동안 참여를 선택한 테스터 **12명**" (검토 시점: 8월 14일 오후 1:47).
  **여전히 정확히 12명, 이탈 없음.** 2일만 더 유지되면 14일 충족 →
  **프로덕션 액세스 신청 가능일 ≈ 2026-08-28** (기존 어림짐작과 일치). "프로덕션 신청" 버튼 아직 비활성.

## 2026-08-28 — 프로덕션 액세스 재신청 제출 🚀
- ✅ **14일 요건 충족 확인** — 콘솔 대시보드의 세 조건이 전부 초록 체크(취소선)로 바뀌고
  **"프로덕션 신청" 버튼이 활성화**됐다. 8/26 시점 "12일차·12명"에서 예상한 8/28과 정확히 일치.
- ✅ **재신청 제출 완료 — 2026-08-28 오후 6:18.** 콘솔 표시:
  "프로덕션 액세스 권한 신청을 접수했습니다 … 일반적으로 7일 이내" → **결과는 메일로 온다고 안내됨**
  (⚠️ 다만 비공개 테스트 게시 때는 메일이 안 왔다 — **콘솔도 같이 볼 것.**)
- 🚨 **폼에 1차 신청(2026-08-13 반려) 때의 답변이 그대로 남아 있었고, 거기에 지어낸 내용이 있었다.**
  `production-access-application.md` §0 이 경고한 바로 그 문제다. 전부 실제 기록으로 교체하고 냈다:
  - (모집 경로) `"프"` 에서 잘린 미완성 문장 → 가족·지인·직장 동료, 카톡·문자, 등록 14명 중 옵트인 12명
  - (참여도) ~~"초등학생 자녀를 둔 지인들이 자녀와 함께 플레이하며 연령대에 맞는 난이도를 확인"~~ →
    12명 14일 유지 + v47 실기기 신고 사례. **"모든 테스터가 10곡을 끝까지 쳤는지는 확인 못 했다"고
    정직하게 명시**(앱이 데이터를 수집하지 않아 알 수 없음).
  - (받은 의견) ~~"터치 반응속도 적절·마을 연출 호평·버튼 위치 작다 3건, 버튼 크기와 터치 영역 조정"~~
    → **전부 근거 없는 창작이었다.** 실제로는 v47 가로 악보 신고 **1건뿐**이라고 그대로 씀 + 의견 이메일 등록 언급
  - (개선 내용) ~~"터치 영역 넓힘", "음악 재생 안정성 점검"~~ → v47 악보 잘림·v48 버튼 크기 통일·
    v49 상단바 이탈·v50 뒤로가기 (전부 이 파일에 기록된 것)
  - (프로덕션 판단) ~~"테스터들이 오류 없이 끝까지 플레이"~~ → 자동 플레이 9판 완주 검증 + S8 실기기 확인
    (개발자가 실제로 한 것으로 바꿔 씀)
  - (게임 대상) ~~"캐릭터 성장 요소"~~ → 없는 기능이라 "마을이 색을 되찾는 진행" 으로
  - (**이번에는 무엇을 다르게 했나** — 재신청 핵심, 원래 **빈칸**이었음) → 1차 반려 사유 2개에 직접 대응:
    ① 비어 있던 의견 이메일에 `sya.apps.dev@gmail.com` 등록, ② 콘솔로 12명 이탈 없이 관리,
    ③ 발견된 문제를 고친 업데이트를 실제 배포·실기기 확인
- 📌 **교훈: 콘솔 폼은 지난 신청 답변을 그대로 들고 있는다.** 재신청 때 "새로 쓰는 칸"이라고
  생각하고 열면 옛 답변이 그대로 제출된다. **반드시 네 단계를 모두 열어 읽고 교체할 것.**
- ⏭ **심사 중에도 테스터 12명을 유지해야 한다.** 통과해도 **앱 심사가 한 번 더** 남는다.

## 2026-08-28 — 홈 화면에 추가한 사람도 저절로 최신이 되게 (v51)
40. ✅ **새 버전 자동 반영** — 웹을 고쳐 push 해도 **홈 화면에 추가한 사람 폰에는 안 닿던 문제.**
    다시 깔지 않으면 옛 화면을 계속 봤다.
    - 원인: 서비스워커는 **HTML 을 네트워크 우선**으로 받으므로 *새로고침만 하면* 최신이 뜬다.
      그런데 **홈 화면 앱(PWA)은 메모리에서 그대로 되살아나 새로고침할 일이 없다.**
      그래서 새 배포가 나와도 받을 기회 자체가 없었다. (`VER` 을 올려도 마찬가지 —
      그건 캐시 서랍 이름일 뿐 페이지를 다시 읽게 만들지는 않는다.)
    - 고침: `index.html` 끝에 **앱이 다시 보일 때마다 서버에 새 버전이 있는지 묻고,
      있으면 조용히 새로고침**하는 코드를 넣었다.
      `visibilitychange`(앱 다시 열기)·`focus`·`pageshow`(iOS 가 메모리에서 되살릴 때) + 30분마다.
    - 비교 방법: **`document.lastModified`(지금 보고 있는 문서가 언제 것인지) vs 서버의
      `Last-Modified` 헤더**(`HEAD` + `cache:'no-store'` 로 캐시를 지나쳐 물어본다).
      → **버전 파일을 따로 두지 않아도 되고, `sw.js` 의 VER 을 손으로 올릴 필요도 없다.**
      (GitHub Pages 가 `ETag`·`Last-Modified` 를 둘 다 준다는 것을 실제로 확인하고 고른 방법)
    - 🚫 **연주 중에는 절대 새로고침하지 않는다.** 아이가 치던 곡이 날아가면 안 된다.
      다음 기회(앱을 다시 열 때, 또는 30분 뒤)에 반영된다.
      ⚠️ 처음엔 "곡이 끝나고 클릭하면 반영" 으로 짰다가 **뺐다** — 아이가 지도에서 다음 사람을
      누르는 순간과 겹치면 그 누름이 통째로 날아간다. 급할 것 없으니 다음 기회를 기다린다.
    - 🌐 **오프라인은 그대로 유지된다** — 물어보는 요청이 실패할 뿐이고 캐시로 논다.
      (나란히는 서비스워커를 아예 껐지만(`--pwa-strategy=none`), 그건 Supabase 연결이 필수라
      오프라인 가치가 낮아서다. **사랑나라는 완전 오프라인 게임이라 SW 를 버리면 안 된다.**)
    - ✅ **검증**: 가짜 서버(`Last-Modified` 를 미래로 미는)로 5가지를 실제 브라우저에서 확인 —
      ① 새 버전 없으면 새로고침 안 함 ② 새 버전 생기면 새로고침함 ③ **다시 확인해도 무한
      새로고침 없음** ④ **연주 중엔 새로고침 안 함** ⑤ 연주 끝나면 그때 반영됨. 전부 통과.
      `document.hidden` 가드도 실제로 도는 것을 확인(비활성 탭에서 요청을 안 보냄).
    - ⚠️ 이미 홈 화면에 추가한 사람은 **다음 콜드 스타트(앱이 메모리에서 완전히 내려간 뒤 실행)**
      때 이 새 코드를 처음 받는다. 그 뒤부터 저절로 최신이 된다. 다시 깔 필요는 없다.
    - 📌 **앱(AAB)에는 영향 없다** — Capacitor 안에서는 이 코드가 아예 안 돈다(`window.Capacitor` 가드).
      앱은 파일을 동봉하므로 예전처럼 `npm run sync` → 버전 올림 → `npm run release` 가 필요하다.

## 2026-08-29 — 아이폰 사파리 확대 방지 실기기 확인 완료 ✅
- ✅ **사용자가 실제 아이폰 사파리로 웹(https://sya-apps.github.io/sarangnara/)을 열어 확인.**
  막아둔 세 갈래 전부 정상 — ① 건반 빠른 더블탭 ② 두 손가락 동시 터치(화음) ③ 핀치.
  **어느 것도 화면이 확대되지 않음.**
- 관련 코드: 뷰포트 `user-scalable=no`(사파리는 무시함) + 건반 `touch-action:manipulation` +
  `touchend` 350ms 가드(버튼은 예외) + `gesturestart/change/end` 취소 +
  `touchstart` 손가락 2개 이상 취소. 네 겹이 같이 걸려 있어 어느 하나가 뚫려도 나머지가 막는다.
- 📌 **아이폰은 앱(AAB)이 없고 웹만 있으므로 이 확인으로 아이폰 쪽은 끝났다.**
  (공유하기의 아이폰 링크가 이 웹 주소를 가리킨다)

## 2026-08-29 — 타이틀에 버전 표시 (v52)
41. ✅ **타이틀 화면 왼쪽 아래에 지금 떠 있는 판을 적는다.** "지금 무슨 버전이야?" 를
    **폰만 보고** 답할 수 있게 하려는 것. 전에는 개발자가 `adb dumpsys package` 로 캐야 알았다.
    - **웹**: `v52 · 08-29 14:17` — 숫자와 날짜를 **같이** 적는다.
      - 숫자(`APP_VER`)는 손으로 올리는 값, 날짜는 `document.lastModified` 에서 **저절로** 나온다.
      - 👉 **둘을 같이 적는 이유**: 숫자만 적으면 올리는 걸 깜빡한 순간부터 화면이 거짓말을
        시작한다. 날짜는 깜빡할 수가 없다. 날짜의 출처가 **자동 업데이트 코드(08-28)가
        "최신인가"를 판단할 때 쓰는 것과 같아서**, 화면의 날짜가 곧 "내가 받아 놓은 판"이다.
    - **앱(Capacitor)**: `앱 1.3(4) · v52` — `@capacitor/app` 의 `App.getInfo()` 로
      네이티브가 아는 versionName(versionCode)을 물어서 적는다(뒤로가기 때 이미 넣은 플러그인).
      - 🚨 **앱에서는 날짜를 절대 안 쓴다.** 앱은 파일을 동봉해 열기 때문에
        `document.lastModified` 가 배포 시각이 아니다 — 그걸 띄우면 묻는 사람을 속인다.
        (처음엔 `Capacitor` 는 있는데 `App` 플러그인만 없을 때 날짜가 새어 나왔다. 검증에서 잡아 고침.)
      - 못 얻으면 숫자만 남긴다. **틀린 것보다 적게 보이는 게 낫다.**
    - 자리: `.legal`(개인정보처리방침) 과 **같은 크기·같은 색으로 반대쪽 구석**.
      `pointer-events:none` — 링크가 아니라서 아이가 구석을 잘못 눌러도 아무 일도 안 일어난다.
    - 📌 **`sw.js` 의 VER 과 `APP_VER` 을 같은 값으로 맞춘다.** 사람이 부르는 버전이
      하나뿐이어야 답이 갈리지 않는다. 이번에 둘 다 v51→**v52**.
    - ✅ **검증**: `sarangnara-app/check-ver.mjs` — `node check-ver.mjs` 한 줄이면
      **서버까지 스스로 띄웠다 내리고** 두 갈래를 본다.
      ① 웹: s8·아이폰·작은폰·아이패드 네 크기에서 보이는지·안 잘리는지·개인정보처리방침/공유/
      출발 버튼과 안 겹치는지·안 눌리는지 ② 앱: `Capacitor` 를 흉내 내어 네이티브 버전이 붙는지,
      getInfo 실패·플러그인 없음일 때도 예외 없이 숫자만 남는지, **날짜가 새어 나오지 않는지**. 전부 통과.
      ⚠️ `file://` 이 아니라 **http 로 열어야** 한다 — `document.lastModified` 가 달라진다.
    - ✅ **아이폰 사파리 실기기 확인 완료**(2026-08-29) — 사용자가 폰에서 열어 왼쪽 아래
      `v52 · 08-29 14:18` 이 보이는 것을 확인. 웹(윈도우 크롬)에서도 서버에 `HEAD` 를 던져
      **화면의 날짜 = 서버의 Last-Modified (차이 0초)** 인 것까지 확인했다.
    - ⚠️ **앱에 넣으려면** 예전처럼 `npm run sync` → 버전 올림 → `npm run release` 가 필요하다.
      지금 스토어에 있는 versionCode 4(1.3)에는 이 표시가 없다.

## 2026-08-30 — 프로덕션 액세스 심사 진행상황 콘솔 직접 확인
- ✅ **Play Console 대시보드/프로덕션 페이지 확인(2026-08-30)** — 상태 그대로:
  "프로덕션 액세스 권한 신청을 접수했습니다 … 신청 양식을 검토하는 중입니다.
  검토 결과는 **이메일을 통해 계정 소유자에게** 알려드립니다. 일반적으로 7일 이내"
  · **금요일(=8/28) 오후 6:18 신청함**. 신청일이 콘솔 표시와 일치.
- ✅ 비공개 트랙 `사랑나라_260721` — **활성 · 최신 출시 버전 4 (1.3) · 게시일 8/17 오후 1:24 ·
  "선택한 테스터에게 제공됩니다"**. 게시 개요 = "게시되지 않은 변경사항이 없습니다"(깨끗).
- ✅ 테스터 탭 — 목록 `SYA 테스터` **등록 14명**.
- ✅ **1차 반려 사유 "의견 수렴 창구 없음" → 조치 완료.** 트랙 테스터 탭의
  `의견 URL 또는 이메일 주소` 칸에 **`sya.apps.dev@gmail.com` 이 들어가 있는 것을
  오늘 눈으로 확인**(22/512자). 1차 신청(2026-08-13) 때 **비어 있던 칸**이고,
  8/28 재신청 전에 채워 넣었다. **더 손댈 것 없는 완료 항목이다.**
- 🚨 **옵트인 인원(현재 참여 N명)은 지금 콘솔 어디에도 안 보인다.** 신청을 접수한 뒤로는
  대시보드의 "14일 더 비공개 테스트 실행 — 현재 N일 동안 참여를 선택한 테스터 N명" 체크리스트가
  **심사 진행 안내 패널로 통째로 바뀌었다.** 8/26 확인(12일차·12명)이 마지막으로 본 숫자다.
  → **심사 중 12명 유지는 여전히 필요한데 콘솔로는 확인할 방법이 없다.** 테스터가 앱을 지우지
  않게 두는 것 말고 할 수 있는 게 없으니, 이 기간엔 테스터에게 아무 것도 요청하지 않는다.
- 📌 **콘솔 접속 경로 메모**: 크롬 기본 프로필의 `u/0` 은 개인 계정이라 열면 *개발자 계정 만들기*
  화면이 뜬다. **`play.google.com/console/u/1/developers` 로 들어가 `SYA_Apps` 를 고를 것.**
- 알림함에 사랑나라 관련 새 항목 없음(나란히 게시 8/29, 메모리 품질요구 8/27, 개발자 인증 8/7).
  관리자 메일함(최근 5일)에도 심사 결과 메일 없음 — AdMob 홍보·Play 뉴스레터·구글 보안알림뿐.
- ⏭ 다음 확인: **9/4 즈음**까지 결과가 안 오면 그때 다시 콘솔+메일.

## 2026-08-30 — 공유하기가 앱에서 "링크 복사"밖에 안 되던 것 고침 (v53)
42. ✅ **앱에서 "친구에게 알려주기" 를 눌러도 카카오톡·문자가 안 뜨고 링크만 복사되던 문제.**
    사용자가 폰에 깔린 Play 설치본에서 신고.
    - 원인: **안드로이드 WebView 에는 Web Share API(`navigator.share`)가 아예 없다.**
      웹에서는 폰 크롬·사파리가 이걸 지원해 OS 공유 시트가 떴지만, **앱은 WebView 라
      `navigator.share` 가 undefined** 였고 코드가 곧장 2단계(클립보드 복사)로 떨어졌다.
      👉 **웹에서 되니까 앱에서도 되겠지, 가 틀린 자리다.** WebView ≠ 크롬.
    - 고침: **`@capacitor/share` 8.0.1** 추가 → 앱에서는 네이티브 `ACTION_SEND` 로 진짜
      공유 시트를 띄운다(카카오톡·문자·메일…). `index.html` 의 `shareApp()` 을 3단 사다리로:
      **① Capacitor Share(앱) → ② `navigator.share`(폰 브라우저) → ③ 클립보드 복사(PC)**
    - 🔒 **권한이 하나도 안 늘었다** — 병합 릴리스 매니페스트를 실제로 뽑아
      **스토어에 올라간 versionCode 4 AAB 와 대조**했다. 양쪽 다
      `com.sya.sarangnara.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` 하나뿐이고
      **`INTERNET` 은 여전히 없다**(0건). 플러그인 자체 AndroidManifest 도 비어 있다.
      (⚠️ 예전에 "uses-permission 0개" 라고 적어 뒀는데, 정확히는 위 **자기 서명 권한 1개**가
       versionCode 1 때부터 있었다. 기기 권한이 아니라 앱이 자기 리시버를 지키는 것이라
       심사·데이터안전에 영향 없음. `android.permission.DUMP` 는 profileinstaller 리시버가
       **부르는 쪽에** 요구하는 것이라 우리가 요청하는 권한이 아니다 — 이것도 전부터 있었다.)
    - 🚫 **취소는 실패가 아니다** — 사람이 공유 시트를 닫으면 복사로 되돌아가지 **않는다**.
      (되돌아가면 "취소했는데 왜 복사됐지?" 가 된다.) 반대로 **진짜 실패**(플러그인 미등록 등)
      일 때는 복사로 되돌아간다 — 아무 일도 안 일어나는 게 제일 나쁘다.
    - `url` 은 여전히 안 넘긴다 — 두 링크(안드로이드 앱 / 아이폰 웹)를 `text` 에 나눠 담았는데
      `url` 까지 주면 받는 앱이 링크 하나만 강조하거나 중복시킨다.
    - ✅ **검증**: `sarangnara-app/check-share.mjs` — `node check-share.mjs` 한 줄이면
      **서버까지 스스로 띄웠다 내리고** 세 환경 × 실패처리 다섯 갈래를 본다.
      ① 앱=플러그인 호출·복사 안 함 ② 폰 브라우저=navigator.share·복사 안 함
      ③ PC=복사+토스트 ④ 취소 시 복사 안 함 ⑤ 진짜 실패 시 복사함. **전부 통과.**
      `check-ver.mjs` 도 다시 돌려 버전 표시가 안 깨진 것 확인(v53).
    - ⚠️ **앱에 닿으려면 새 AAB 가 필요하다.** 지금 스토어의 versionCode 4(1.3)에는 없다.
      **프로덕션 액세스 심사 중이라 업로드는 통과 후에** — 아래 '옮기는 절차' 참고.
      웹(GitHub Pages)은 push 하면 바로 반영된다.

## 2026-08-31 — 🚨 프로덕션 액세스 **2차 신청도 반려**

- 📬 **관리자 메일함에서 확인(2026-08-31 확인)** — `Google Play Support`,
  **2026-08-30 17:14 PDT 수신(= KST 8/31 오전 9:14)**, 제목
  **"Google Play 프로덕션에 액세스하려면 추가 테스트 필요"**.
  8/30 에 콘솔을 봤을 때는 아직 "검토 중" 이었는데, **그날 밤에 결과가 온 것**이다.
- **반려 사유 — 1차(8/13)와 글자 그대로 똑같다**:
  1. "비공개 테스트 중에 테스터가 앱에 참여하지 않았습니다."
  2. "앱 업데이트를 통해 사용자 의견을 수집하고 조치를 취하는 등의 테스트 권장사항을
     따르지 않았습니다."
  3. "다시 신청하기 전에 **실제 테스터를 대상으로 14일간** 비공개 테스트를 진행하세요."
- 🚨 **교훈: 답변을 정직하게 고쳐 쓴 것만으로는 안 됐다.** 2차 때 창작을 전부 걷어내고
  실제 기록으로 채웠고, 1차 사유 2번의 직접 원인이던 **의견 이메일 칸도 채워서** 냈다.
  그런데 **같은 두 문장**이 돌아왔다. → 심사가 보는 것은 **답변의 질이 아니라 구글이
  관측한 테스터의 실제 활동**일 가능성이 높다. **옵트인 12명·14일은 최소 조건일 뿐,
  "실제로 앱을 켰는가"는 별개다.** 우리 앱은 인터넷 권한조차 없어 사용 데이터를 안 보내지만
  **Play 는 설치·실행 자체를 안다.**
- ⚠️ **메일에 앱 이름이 안 적혀 있다.** 계정에 앱이 둘이라 확정은 콘솔로 해야 한다.
  다만 다른 앱은 8/29 에 비공개 테스트가 막 게시돼 신청 자체가 불가능했고, 사랑나라는
  8/28 오후 6:18 신청 → **사랑나라 건으로 본다.**
- ⚠️ **14일 카운트가 또 리셋됐는지 콘솔에서 확인해야 한다.** 1차 반려 때는 리셋됐다.
- 📌 스토어에서 **검색이 되는 것은 정식 출시가 아니다.** 옵트인한 테스터 계정에는 비공개
  테스트 앱이 스토어에 보인다. 실제로 `play.google.com/store/apps/details?id=com.sya.sarangnara`
  는 **비로그인 상태에서 여전히 404**(2026-08-31 확인) — **아직 일반 공개 아님.**
- ⏭ 다음 신청 전에 바꿀 것은 **문구가 아니라 테스트 실체**다:
  ① 테스터가 14일 동안 **여러 날 실제로 플레이**하게 부탁하기,
  ② **의견을 받은 기록**(메일 한 줄이라도) → 그걸 고친 **업데이트 배포**의 짝 만들기 —
  v53 공유하기 수정이 바로 그 사례가 될 수 있다(신고 → 고침. 아직 앱에 안 올라갔을 뿐),
  ③ 그 기간에 **versionCode 5(1.4)** 를 올려 "의견 → 업데이트" 를 증명.
  자세한 것은 `sarangnara-app/docs/production-access-application.md` §0.6.

## 2026-08-31 — 🔎 콘솔 직접 확인: **반려 사유가 숫자로 확인됐다**

Play Console 을 직접 열어 봤다. 반려는 사실이고, **왜 반려됐는지가 통계에 그대로 보인다.**

**대시보드(사랑나라)**
- "Google Play 프로덕션에 액세스하려면 게임에 추가 테스트가 필요합니다 …
  **오늘, 오전 9:14에 검토되었습니다**" — 메일 도착 시각과 일치.
- 체크리스트 3줄 중 앞의 둘(비공개 테스트 버전 게시 / 12명 이상 참여 선택)은 초록 취소선,
  **세 번째 "검토일부터 12명 이상의 테스터를 대상으로 14일 더 비공개 테스트 실행" 만 미완**.
  → **14일 카운트가 또 리셋됐다. 시작점은 검토일(8/31)이다 → 다음 신청 가능 ≈ 2026-09-14.**
- 「프로덕션 신청」 버튼 회색(비활성). 아직 "현재 N일차·N명" 숫자는 안 뜬다(오늘 리셋돼서).

**🚨 통계 — 여기가 진짜 이유다**
- **일일 활성 사용자(DAU)**: 8/3~8/21 사이에 **점이 딱 두 개.**
  **8/14 에 1명, 8/17 에 2명. 나머지 날은 전부 0명.**
- **월간 활성 사용자(MAU, 28일 내 한 번이라도 앱을 연 사람)**: 8/13 까지 **1명**,
  8/14 에 3명, 8/17 부터 **4명**으로 8/21 까지 그대로.
  → **14일 테스트 기간 내내 앱을 한 번이라도 연 사람이 통틀어 4명뿐이다.**
  그 4명에는 개발자 본인 기기(갤S8 확인 8/3·실기기 뒤로가기 검증 8/18)도 들어 있을 것이다.
- **설치한 사용자 수**: 8/25 기준 **11명**(기간 중 10~13 사이 오르내림).
  옵트인 12명은 맞았지만 **설치는 11명**이었고, **그중 앱을 연 사람은 4명**이었다.

**결론 — 관문이 하나가 아니라 둘이었다 (2026-08-31 구글 공식 문서로 확인)**

출처: Play Console 고객센터 "App testing requirements for new personal developer accounts"
(`support.google.com/googleplay/android-developer/answer/14151465`).

| | 무엇 | 확인 방법 | 우리 상태 |
|---|---|---|---|
| **① 자동 요건** | "a minimum of 12 testers who have been **opted in** continuously for at least 14 days" — **옵트인만** 하면 된다. 실행은 안 따진다 | 콘솔 체크리스트·「프로덕션 신청」 버튼 활성화 | **통과했다**(8/28 버튼 열림) |
| **② 사람이 보는 심사** | "Reasons for required continued testing include having fewer than 12 opted-in testers **or insufficient tester engagement** during the testing period." 신청서 1부에서 **"testers used all available app features"**, **"tester usage matched expected production user behavior"** 를 본다 | 신청서 답변 + 구글이 관측한 사용 | **여기서 떨어졌다** |

> **그래서 ①만 채우고 있었던 것이다.** ② 는 "몇 명이 몇 번" 이라는 **숫자 기준을 구글이 공개하지 않는다** —
> 기준 문장은 **"실제 출시 후 사용자처럼 썼는가"** 다. 12명이 옵트인했는데 앱을 연 사람이 4명,
> 대부분의 날 DAU 0 이면 어느 쪽으로 읽어도 여기에 못 미친다.
> 우리 앱은 인터넷 권한도 없어 데이터를 안 보내지만 **Play 는 실행 자체를 안다.**
> **1차·2차 반려 사유가 글자 그대로 같았던 이유가 이것이다 — 답변의 문제가 아니었다.**

🚨 **직접 원인은 우리가 테스터에게 보낸 안내문이었다.** 그 안내문에 이렇게 적혀 있었다:
> "구글 정책상 12명이 14일 동안 앱을 **지우지 않고** 두어야 정식 출시가 가능해요.
> **자주 안 쓰셔도 괜찮으니** 지우지만 말아 주세요."

**①만 알고 ②를 몰랐으니 "안 써도 된다" 고 안내한 것이고, 테스터들은 그대로 따랐다.**
DAU 0 은 테스터 탓이 아니라 **우리 안내문 탓이다.** 구글 문서의 "Tester engagement" 항목은
정확히 그 반대를 요구한다 — *"Provide beta testers with clear instructions on how to test your app
and report bugs."* **3차 테스트를 시작하기 전에 안내문부터 고쳐야 한다.**

**그래서 3차 전에 해야 할 일은 하나뿐이다**: **테스터 12명이 14일 동안 실제로 앱을 켜게 하는 것.**
- 목표는 "설치했다" 가 아니라 **DAU 가 매일 두 자리에 가깝게 찍히는 것**이다.
- 확인 방법: Play Console → **통계 → 측정항목 `참여도 → 일일 활성 사용자(DAU)`**.
  이 화면을 **며칠에 한 번 직접 볼 것.** 대시보드 체크리스트만 보면 또 속는다.
  (⚠️ 통계는 3~5일 늦게 채워진다 — 8/31 에 봤을 때 DAU 는 8/21, 설치는 8/25 까지만 나왔다.)
- 의견도 마찬가지다. **의견 이메일 칸을 채운 것만으로는 "의견 수렴" 으로 안 쳐준다.**
  실제로 받은 메일 → 그걸 고친 새 버전 배포의 짝을 이 기간에 만들어야 한다.
  v53 공유하기 수정이 그 사례가 될 수 있다(신고받아 고쳤다. 아직 앱에 안 올렸을 뿐).

**같은 계정의 다른 앱은 이미 프로덕션 상태다** — 즉 이 반려는 사랑나라 건이 맞다(앱 목록에서 확인).

## 2026-08-31 — 테스터 안내 페이지 `install.html` 추가

카톡 문구가 길고, 안드로이드·아이폰 안내가 섞여 있어 사람이 자기 것만 골라 읽기 어려웠다.
**링크 하나를 누르면 자기 기기에 맞는 안내가 뜨는 페이지**를 만들었다.
→ `https://sya-apps.github.io/sarangnara/install.html`

- **기기 판별해서 맞는 탭을 먼저 연다.** 안드로이드=옵트인 링크로 앱 설치,
  아이폰=웹 열기 + 홈 화면 추가. **탭은 늘 둘 다 보이게 뒀다** — 자동 판별은 틀릴 수 있고,
  틀렸을 때 빠져나갈 길이 없으면 안내문이 아니라 벽이 된다.
- 🍎 **아이패드는 iPadOS 13+ 부터 UA 가 맥이라 `iPad` 로 안 잡힌다** → `Macintosh` +
  `ontouchend` 있음으로 가려낸다. 이걸 안 하면 아이패드가 안드로이드 안내를 본다.
- 💬 **카톡 인앱 브라우저(아이폰)에는 '홈 화면에 추가' 가 아예 없다.** 그래서 카톡 안에서 열면
  "⋯ → Safari로 열기" 안내가 자동으로 뜬다. **이걸 안 적으면 아이폰 쪽은 십중팔구 막힌다.**
- 스타일은 `privacy.html` 과 같은 팔레트(라이트/다크 둘 다), 제목은 게임과 같은 Gaegu.
- 카톡 공유 미리보기용 OG 태그도 넣었다(`og:url` 은 이 페이지, 이미지는 기존 `og-image.png`).
- ⚠️ **스토어 링크(`store/apps/details?id=...`)는 아직 404 다.** 이 페이지도 옵트인 링크만 쓴다.
- `sw.js` 의 `FILES` 에는 안 넣었다 — HTML 은 네트워크 우선이라 그냥 열린다. VER 도 안 올렸다.

**부탁 문구의 수위는 사용자가 정했다(2026-08-31)**: "14일 동안 지우지 말 것"(필수) +
"시간 나실 때 한두 번 체험"(부탁). 무리한 부탁으로 옵트인이 깨지면 14일이 리셋돼 더 비싸다는 판단.
⚠️ 다만 **12명 × 한두 번이면 DAU 1~2 수준**이라 구글 기준에 닿을지는 알 수 없다.
→ **9월 초에 통계(참여도 → 일일 활성 사용자)를 보고 낮으면 한 번 더 독려할 것.**

카톡에 붙여넣을 문구는 `C:/SYA/tools/사랑나라-테스터-안내문.md` 에 있다(공용, git 아님).
**나란히 문구는 나란히 프로젝트 폴더에서 따로 만든다** — 이 세션에서 건드리지 않았다.

## 2026-08-31 — 🔒 **누가 설치했는지는 콘솔로 알 수 없다** (확인 완료)

"이미 깐 사람에겐 설치 안내를 빼고 보내자" 는 생각으로 콘솔을 뒤졌다. **방법이 없다.**

- 비공개 테스트 트랙 → 테스터 탭 → 목록 `SYA 테스터` 를 열면 **이메일 주소와 삭제 버튼뿐**이다.
  사람별 **설치 여부·옵트인 여부·실행 여부를 보여 주는 칸이 어디에도 없다**(14명 등록).
- 콘솔이 주는 건 **합계뿐**이다 — 설치한 사용자 수 11, DAU, MAU. 누가 그 11명인지는 안 알려준다.
- 👉 그래서 **문구로 해결한다**: 기존/신규 두 벌로 나누고, **받는 사람이 스스로 자기 경우를
  고르게** 쓴다("아직 깔려 있는지 봐 주세요 / 지워졌으면 아래 링크로").
- ⚠️ 테스터 이메일 주소는 **이 파일에도, 어디에도 옮겨 적지 않는다** — 이 저장소는 공개다.
  원본은 콘솔의 테스터 목록이 유일한 정본.

**카톡 문구는 두 벌이 됐다**(`C:/SYA/tools/사랑나라-테스터-안내문.md`):
① **기존 테스터용** — 설치 유지 확인 + "오늘부터 14일, 시간 나실 때 한두 번 열어서 한 곡만"
② **신규 테스터용** — 원래 쓰던 ①②③ 설치 안내 그대로 + 아이폰 웹 링크 한 줄 추가

- **안드로이드는 옵트인 링크로 바로 보낸다.** `install.html` 을 한 단계 끼우는 것보다
  바로 설치로 가는 게 낫다는 사용자 판단(2026-08-31). 페이지는 지우지 않고 남겨 둔다.
- ⚠️ **신규 테스터는 콘솔 이메일 목록에 먼저 넣어야 한다.** 안 넣고 링크만 보내면
  "테스터가 아닙니다" 가 뜬다.

## 2026-09-01 — 3차 테스트 D+1 콘솔 확인 + versionCode 5(1.4) 빌드

**콘솔에서 직접 본 숫자 (2026-09-01)**
- **설치한 사용자 수 = 11명.** 8/31 과 같다(앱 목록 화면). 늘지도 줄지도 않았다.
- **DAU(참여도 → 일일 활성 사용자)**: 그래프에 점이 여전히 **딱 두 개 — 8/14 에 1명, 8/17 에 2명.**
  나머지 날은 전부 0. **8/31 에 본 것과 똑같다.**
  ⚠️ 다만 **통계가 8/23 까지밖에 안 채워져 있다.** 새 안내문을 8/31 에 보냈으니
  그 효과는 **9/3~9/5 쯤에야 그래프에 나타난다.** 지금 0 인 것은 아직 판단 근거가 아니다.
- 대시보드 체크리스트: 앞의 두 줄 초록 취소선, **세 번째 "검토일부터 12명 이상의 테스터를
  대상으로 14일 더 비공개 테스트 실행" 만 미완.** 「프로덕션 신청」 버튼 회색.
  → 8/31 리셋 그대로. **다음 신청 가능 ≈ 2026-09-14.**
- 테스터 **1명 추가 등록 → 등록 15명**(사용자가 오늘 등록). 여유 3명.
  ⚠️ **등록 ≠ 옵트인.** 옵트인 링크를 눌러야 카운트에 들어간다.

**📌 반려 사유는 어디서 보는가 (2026-09-01 확인)**
| | 보이는 것 |
|---|---|
| **Play Console 대시보드** | *"프로덕션에 액세스하려면 게임에 추가 테스트가 필요합니다"* + **검토 시각**(월요일 오전 9:14) + 체크리스트 3줄 + 회색 「프로덕션 신청」 버튼. **사유 목록은 없다.** |
| **관리자 메일**(`--body 44`) | **사유 3줄이 여기에만 있다** — ① 테스터가 앱에 참여하지 않음 ② 의견 수집·조치 권장사항 미준수 ③ 실제 테스터로 14일 재시행 |
| **알림함(🔔)** | 품질 요구사항·개발자 인증 안내뿐. **반려 알림은 안 들어온다.** |

> 👉 **콘솔은 "떨어졌다"까지만 알려주고, "왜"는 메일에만 온다.** 둘 다 봐야 한다.
> 진짜 원인(DAU)은 어느 쪽에도 안 적혀 있고 **통계에서 직접 읽어야** 나온다.

**🚀 versionCode 5 (versionName 1.4) AAB 빌드 완료 — 사람이 콘솔에 올리는 것만 남음**
- 파일: `sarangnara-app/android/app/build/outputs/bundle/release/app-release.aab` (2026-09-01 빌드)
- 검증: 매니페스트 **versionName 1.4**, `jarsigner -verify` → **jar verified**,
  동봉된 웹 스냅샷 **APP_VER = 'v53'**(공유하기 수정 포함), `uses-permission` **여전히 0개**
  (`androidx.profileinstaller` 의 `DUMP` 만 — versionCode 1~4 에도 있던 것, 우리가 요청하는 권한 아님).
- ⚠️ **직전 AAB(8/17 빌드)에는 `APP_VER` 이 아예 없었다** = v52 이전(v50) 스냅샷이었다.
  즉 웹은 v53 인데 **앱은 세 버전 뒤처져 있었다.** 이번 빌드로 맞췄다.
- 💡 이 업로드가 **"의견 → 업데이트" 증거**가 된다(공유하기 안 된다는 신고 → v53 수정 → 1.4 배포).
  3차 신청서 Q3 에 이 짝을 쓴다.

**앱·웹 기능 일치 최종 확인 (md5 로 대조)**
- `sarangnara-web/index.html` = `sarangnara-app/www/index.html`
  = `sarangnara-app/android/app/src/main/assets/public/index.html` — **셋 다 같은 해시**.
- 이제 AAB 안까지 v53. **웹과 앱이 같은 코드로 맞았다.**

## 다음 확인/할 일
- [x] **심사 결과 확인 완료**(2026-07-24) — **메일이 아니라 Play Console 에서** 확인. 위 07-24 항목 참고.
      (메일 훑기는 `python C:\SYA\tools\check_admin_mail.py --app 사랑나라`, 결과는 관련 메일 0통)
- [x] 🚨 **테스터 12명 이상 옵트인 — 달성**(2026-07-25 확인) 🎉
      - 2026-07-24: 안내문 발송. 옵트인 1명 → 5명 → **2026-07-25 에 12명 도달 확인**
      - ✍️ **12명을 넘긴 날짜 = 2026-07-25** ← 14일 카운트 시작일
      - ✅ **프로덕션 액세스 신청 가능일 = 2026-08-08 즈음** (7/25 + 14일. 정확한 날짜는 Play Console 이 표시)
      - 확인처: Play Console **대시보드** → "프로덕션 액세스 신청" 의 *현재 참여를 선택한 테스터 N명*
      - 📊 **2026-08-01 확인: "6일 동안 참여한 테스터 12명"** — 정상 진행. 리셋 없음.
        대시보드는 **완료된 날만 세서 실제 경과보다 하루 적게** 보인다(7/25 시작 → 8/1 에 6일).
        → 14일 충족은 **8/8 저녁~8/9**.
        🚨 **옵트인이 정확히 12명이라 여유가 0명이다.** 등록은 14명인데 2명이 아직 옵트인을 안 했다.
        한 명만 빠져도 11명이 되어 **카운트가 0부터 다시 시작**한다. 남은 2명을 채워 두는 게 안전망.
      - 🚨 **이제부터 14일간 12명 밑으로 떨어지면 카운트가 리셋된다.** 등록은 14명이라 여유가 **2명뿐**.
        테스터가 앱을 지우거나 테스트를 그만두면 그만큼 깎인다 → **며칠에 한 번 대시보드 숫자를 볼 것.**
        떨어지면 처음부터 다시 14일이므로, 지금 시점의 인원 이탈이 가장 비싸다.
- [x] **테스터 14명 확보 완료** (2026-07-22) — 목록 등록 기준. 요건(최소 12명) 충족, 2명 여유. 옵트인 링크:
      `https://play.google.com/apps/testing/com.sya.sarangnara`
      → **등록 ≠ 옵트인.** 12명 이상이 실제로 참여를 선택하고 14일 연속 유지해야 한다.
- [ ] 본인 폰(개발자 개인 계정)으로 설치해 실제 동작 확인 — **지금 바로 가능**(7/21부터 배포 중)
- [ ] **12명 이상 × 14일 연속 유지 중**(2026-07-25 시작 ~ **2026-08-08** 즈음) → 프로덕션 액세스 신청 → 정식 출시.
      (~~14명 2026-07-22 확보 → 8/5 즈음~~ 은 옵트인을 마친 줄 알고 계산한 것. 기준은 **12명이 옵트인한 날 + 14일**)
      - 8/8 즈음 할 일: 프로덕션 액세스 신청 → 승인되면 **프로덕션 릴리스** 만들기
        (출시 노트는 `sarangnara-app/store-assets/출시노트.md` 에 versionCode 1·2용 둘 다 준비돼 있음)
      - 공개 출시되면 **공유하기의 안드로이드(Play) 링크가 그때부터 열린다**(지금은 404).
- [x] **태블릿 스크린샷 16:9 재생성 완료**(2026-07-22) — `sarangnara-app/store-assets/tablet/*.png`
      4장을 **2048x1152(16:9)** 로 교체(전엔 16:10이라 Play 크롭 프리셋과 안 맞아 뺐었음).
      전용 스크립트 `sarangnara-app/make-tablet-16x9.mjs`(폰·피처그래픽은 안 건드림).
      **아직 Play Console 에 업로드는 안 함** — 사람이 콘솔에서 태블릿 스크린샷란에 올려야 함.
- [x] **출시 노트 초안 작성**(2026-07-22) — `sarangnara-app/store-assets/출시노트.md`.
      승인 후 **프로덕션 릴리스 만들 때** '출시 노트'란에 붙여넣는다. versionCode 1·2용 둘 다 있음.
- ⛔ ~~심사 중(지금)엔 Play Console 을 건드리지 말 것~~ → **2026-07-24 승인 확인으로 해제.**
      이제 태블릿 스크린샷 업로드·스토어 등록정보 손보기는 해도 된다(변경분은 다시 검토를 타지만
      게시된 릴리스는 그대로 살아 있다). 다만 **새 AAB(versionCode 2)만은 14일을 채운 뒤**에 —
      위 '옮기는 절차' 참고.
      ⚠️ 위에 적어뒀던 "in-review 릴리스는 '수정'이 안 뜨고 '새 버전 만들기'만 뜬다"는 **지금 정반대**다.
      심사가 끝난 지금은 트랙에 빈 임시 버전이 남아 있어 `새 버전 만들기` 가 잠기고 `버전 수정` 만 열린다.
- [ ] **수익화는 실사용자 수를 보고 결정** — 2026-07-24 **보류 결정**.
      지금 앱은 `uses-permission` **0개**(인터넷 권한조차 없음, 외부 요청 0건) + 아동 대상(13세 미만,
      **Families 정책**) + 광고 없음·광고ID 없음·데이터 수집 안 함 선언. 이 구성 덕에 하루 만에 통과했다.
      광고를 붙이면 `INTERNET` 권한·AdMob SDK·앱 콘텐츠 선언 3개 갱신·개인정보처리방침 재작성이
      전부 따라오고, **아동 앱은 개인맞춤 광고 금지라 단가가 가장 낮은 구간**이다.
      Families 정책 위반은 경고가 아니라 앱 정지로 간다. **지금 규모에선 얻는 것보다 잃는 게 크다.**
      → 정식 출시 후 실사용자 수를 보고 다시 판단. 수익화를 한다면 광고보다
      **곡 팩 유료 확장**(아동 앱이라 결제 앞에 **부모 게이트** 필수)이 정책 리스크·단가 양쪽에서 낫다.
      위 '현재 앱 상태(심사 관점)' 의 광고 경고와 같은 맥락.
- [x] 🚀 **versionCode 3(1.2) AAB 를 Play Console 비공개 트랙에 올리기**(2026-08-03 빌드,
      2026-08-09 업로드·검토 제출 완료). 트랙 `사랑나라_260721` → `버전 수정` → AAB 업로드 →
      출시 노트 → 저장 → 게시 개요에서 **"검토를 위해 변경사항 전송"**. 경고 1개(가독화 파일
      미포함, versionCode 2 때와 동일)뿐, 오류 0. 상태 = **"검토 중인 변경사항"**.
      게시 확인되면 versionCode 2(1.1)까지 기준으로 써 둔 나란히식 프로덕션 액세스 신청서
      (`sarangnara-app/docs/production-access-application.md`) Q3 에 이 버전 몫 문단을 추가할 것.
- [x] **연주 화면에서 안드로이드 뒤로가기 처리 — 구현·배포·실기기 검증 전부 완료(2026-08-18).**
      위 '2026-08-18' 항목 참고. 연주→지도, 지도→타이틀, 타이틀→종료 세 갈래 모두 실기기에서 확인됨.
- [x] **아이가 실제로 쳐보고 박자 최종 확인 완료**(2026-08-24) — 등대지기·나비야 포함, 문제 없음.
- [x] ~~🚀 **프로덕션 액세스 심사 결과 기다리는 중**~~ → **2026-08-31 반려 확인**(위 08-31 항목). 아래는 신청 당시 기록.
- [ ] 🚨 **3차 신청 준비 — 문구가 아니라 테스트 실체를 바꿀 것**(위 08-31 두 항목 참고).
      - **14일 카운트는 8/31(검토일)에 리셋됐다 → 다음 신청 가능 ≈ 2026-09-14.** (콘솔이 정확한 날을 알려준다)
      - 🔴 **테스터에게 보내는 안내문부터 고칠 것** — 기존 안내문이 "자주 안 쓰셔도 괜찮다" 고
        말하고 있었다. 이게 DAU 0 의 직접 원인이다. 새 안내문은 **며칠에 한 번 열어서 실제로
        해 보고, 이상하면 의견 메일을 달라**는 요청이 들어가야 한다.
      - 🔴 **핵심: 테스터가 실제로 앱을 켜게 하는 것.** 8월 테스트 기간의 **DAU 는 거의 매일 0**,
        앱을 한 번이라도 연 사람이 **통틀어 4명**이었다. 이걸 안 바꾸면 3차도 같은 사유로 떨어진다.
        (구글은 "몇 명이 몇 번" 이라는 숫자를 안 밝힌다 — 기준 문장은 "실제 출시 후 사용자처럼 썼는가".
        그러니 **소수가 많이** 보다 **12명이 골고루** 쪽으로 맞추는 게 안전하다.)
      - 확인처: 콘솔 → **통계 → `참여도 → 일일 활성 사용자(DAU)`** (통계는 3~5일 지연).
      - 🔴 **의견을 실제로 받은 기록 → 그걸 고친 업데이트 배포**의 짝을 이 기간에 만들 것.
      - versionCode 5(1.4, v53 공유하기)를 이 기간에 올려 "의견 → 업데이트" 를 증명한다.
- (참고, 신청 당시 기록) 🚀 **프로덕션 액세스 심사 결과 기다리는 중**(2026-08-28 오후 6:18 신청, 보통 7일 이내 →
      **2026-09-04 즈음**). 결과는 메일로 온다고 안내받았지만 **콘솔 대시보드도 같이 볼 것**
      (비공개 테스트 게시 때는 메일이 안 왔다). 심사 중에도 **테스터 12명 유지 필수.**
      - 📊 **2026-08-30 확인: 아직 "검토 중". 변화 없음** (위 08-30 항목).
        ⚠️ 신청 접수 뒤로는 **옵트인 인원 숫자가 콘솔에 안 나온다** — 마지막으로 본 값이
        8/26 의 "12일차·12명". 심사 기간엔 숫자를 볼 수 없다는 것을 알고 있을 것.
      - 통과하면: **앱 심사가 한 번 더** 남는다 → 프로덕션 릴리스 만들기
        (출시 노트 `sarangnara-app/store-assets/출시노트.md` — versionCode 3·4용 문단 추가 필요)
      - 공개 출시되면 공유하기의 **안드로이드(Play) 링크가 그때부터 열린다**(지금은 404).
      - 반려되면 즉시 `sarangnara-app/docs/production-access-application.md` §0 에 사유를 남길 것.
- [x] **아이폰 사파리 확대 현상 — 사라진 것 확인 완료**(2026-08-29). 더블탭·두 손가락 동시·핀치
      셋 다 확대 안 됨. 위 '2026-08-29' 항목 참고.
- [x] 🚀 **공유하기 고침(v53)을 앱에 넣기 — versionCode 5(1.4) 빌드 완료(2026-09-01).**
      2차 반려로 심사가 끝났으므로 더 미룰 이유가 없다. 오히려 이 기간에 올려야 "의견 → 업데이트" 증거가 된다.
      **남은 것: 사람이 Play Console 비공개 트랙에 이 AAB 를 올리는 것.** (아래는 당시 기록)
- [ ] ~~공유하기 고침(v53)을 앱에 넣기 — 심사 통과 후 새 AAB 로.~~
      `@capacitor/share` 는 이미 설치·`cap sync` 까지 끝났고 권한도 안 늘어난 것을 확인했다.
      남은 것은 `versionCode 4→5`(`versionName 1.3→1.4`) 올리고 `npm run release` → 업로드.
      **지금은 프로덕션 액세스 심사 중이라 올리지 않는다.**
- [ ] (선택) 곡별 힌트 그림·모드 문구 등 아이 반응 보고 다듬기

## 참고 (기술 메모)
- 곡 데이터: `index.html` 안 `const SONGS=[...]` 배열. 각 곡 `notes:[['음이름',박],...]`,
  쉼표는 `['R',박]`. `sig:[분자,분모]`, `pickup`(못갖춘마디 박), `perSystem`(한 줄 마디수).
- 6/8은 한 마디 3.0박(8분음표 6개), 4/4는 4.0박. buildMeasures 가 박 합으로 마디를 나눔.
- 폰트 재-subset 스크립트/방법은 세션 스크래치패드에 있었음(원본 구글폰트에서 재생성).
- 악보를 이미지로 확인시켜줄 때: 게임의 drawNote/drawBeams 로직을 그대로 옮긴
  Node 스크립트로 SVG 생성해서 보여줌.
- 점검 도구(`sarangnara-app/`): `inspect-phone.mjs`(폰 웹뷰 상태 한 번 훑기),
  `walk-phone.mjs`(폰에서 타이틀→지도→대화→연주까지 눌러가며 화면마다 점검),
  `shot-play.mjs`(패드·아이폰·S8 크기로 **모든 곡 × 연습/전체 모드** 연주 화면 레이아웃 검사).
  폰에 붙는 법: 디버그 APK 설치 → `adb shell pidof com.sya.sarangnara` →
  `adb forward tcp:9222 localabstract:webview_devtools_remote_<pid>`.
  **화면이 꺼져 있으면 innerWidth 가 0 이라 측정이 다 0 으로 나온다** — 깨워 놓고 잴 것.
